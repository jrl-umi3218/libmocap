ADD_LIBRARY(
  mocap SHARED
  marker-set.cc
  )
SET_TARGET_PROPERTIES(
  mocap PROPERTIES
  VERSION ${PROJECT_VERSION}
  SOVERSION 0.0.0
  )
INSTALL(TARGETS mocap DESTINATION ${CMAKE_INSTALL_LIBDIR})
